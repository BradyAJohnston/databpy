# object.create_bob { #databpy.object.create_bob }

```python
object.create_bob(
    vertices=None,
    edges=None,
    faces=None,
    name='NewObject',
    collection=None,
)
```

Create an object but return it wrapped as a BlenderObject